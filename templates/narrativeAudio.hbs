<div class="narrativeAudio-inner component-inner">
    {{> component this}}
    <div class="narrativeAudio-widget component-widget {{#if _hasNavigationInTextArea}}narrativeAudio-text-controls{{/if}}">
        <div class="narrativeAudio-content">
            <div class="narrativeAudio-content-inner">
                {{#each _items}}
                <div class="narrativeAudio-content-item {{#if _isVisited}}visited{{/if}}" data-index="{{@index}}">
                    {{#if title}}
                    <div class="narrativeAudio-content-title">
                        <div class="narrativeAudio-content-title-inner" {{a11y_attrs_heading 'componentItem'}}>
                            {{{title}}}
                        </div>
                    </div>
                    {{/if}}
                    {{#if _graphic.alt}}{{{a11y_aria_image _graphic.alt}}}{{/if}}
                    <div class="narrativeAudio-content-body">
                        <div class="narrativeAudio-content-body-inner">
                            {{{compile body}}}
                        </div>
                    </div>
                    {{#if instruction}}
                    <div class="narrativeAudio-content-instruction">
                        <div class="narrativeAudio-content-instruction-inner">
                            {{{compile instruction}}}
                        </div>
                    </div>
                    {{/if}}
                </div>
                {{/each}}
                <div class="narrativeAudio-controls-container clearfix">
                    <div class="narrativeAudio-indicators">
                        {{#each _items}}
                        <div class="narrativeAudio-progress component-item-color component-item-border {{#if _isVisited}}visited{{/if}}" data-index="{{@index}}"></div>
                        {{/each}}
                    </div>
                    <button class="base narrativeAudio-controls narrativeAudio-control-left" aria-label="{{_globals._accessibility._ariaLabels.previous}}">
                        <div class="icon icon-controls-left"></div>
                    </button>
                    <button class="base narrativeAudio-controls narrativeAudio-control-right" aria-label="{{_globals._accessibility._ariaLabels.next}}">
                        <div class="icon icon-controls-right"></div>
                    </button>
                </div>
            </div>
        </div>
        <div class="narrativeAudio-strapline">
            <div class="narrativeAudio-strapline-header">
                <div class="narrativeAudio-strapline-header-inner clearfix" style="width: {{_totalWidth}}%">
                    <div></div>
                    {{#each _items}}
                    <button class="base narrativeAudio-strapline-title {{#if _isVisited}}visited{{/if}}" aria-label="{{{strapline}}}" style="width: {{../_itemWidth}}%" data-index="{{@index}}">
                        <div class="narrativeAudio-strapline-title-inner h5">
                            {{{strapline}}}
                        </div>
                        <div class="icon icon-plus"></div>
                        <div class="focus-rect"></div>
                    </button>
                    {{/each}}
                </div>
            </div>
        </div>
        <div class="narrativeAudio-slide-container">

            <button class="base narrativeAudio-controls narrativeAudio-control-left" aria-label="{{_globals._accessibility._ariaLabels.previous}}">
                <div class="icon icon-controls-left"></div>
            </button>
            <button class="base narrativeAudio-controls narrativeAudio-control-right" aria-label="{{_globals._accessibility._ariaLabels.next}}">
                <div class="icon icon-controls-right"></div>
            </button>

            <div class="narrativeAudio-slider clearfix" style="width: {{_totalWidth}}%">
                {{#each _items}}
                <div class="narrativeAudio-slider-graphic {{#if _isVisited}}visited{{/if}}" style="width: {{../_itemWidth}}%" data-index="{{@index}}">
                    <img src="{{_graphic.src}}" aria-hidden="true" />
                    {{#if _graphic.attribution}}
                        <div class="graphic-attribution">{{{_graphic.attribution}}}</div>
                    {{/if}}
                </div>
                {{/each}}
            </div>
            <div class="narrativeAudio-indicators">
                {{#each _items}}
                <div class="narrativeAudio-progress component-item-color component-item-border {{#if _isVisited}}visited{{/if}}" data-index="{{@index}}"></div>
                {{/each}}
            </div>
        </div>
        <div class="clearfix"></div>

    </div>
</div>
